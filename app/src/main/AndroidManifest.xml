<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.asiaproxy.trader"
    android:versionCode="1"
    android:versionName="1.1">

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:theme="@android:style/Theme.Material.Light.DarkActionBar"
        android:allowBackup="true">

        <activity
            android:name=".ui.HomeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.PairsActivity"
            android:label="@string/tab_pairs"
            android:parentActivityName=".ui.HomeActivity" />

        <activity
            android:name=".ui.PairDetailActivity"
            android:label="@string/title_pair_detail"
            android:parentActivityName=".ui.PairsActivity" />

        <activity
            android:name=".ui.CalendarActivity"
            android:label="@string/tab_calendar"
            android:parentActivityName=".ui.HomeActivity" />

        <activity
            android:name=".ui.RulesActivity"
            android:label="@string/tab_rules"
            android:parentActivityName=".ui.HomeActivity" />

        <activity
            android:name=".ui.JournalActivity"
            android:label="@string/tab_journal"
            android:parentActivityName=".ui.HomeActivity" />

        <activity
            android:name=".ui.WebActivity"
            android:label="@string/title_web"
            android:parentActivityName=".ui.HomeActivity" />
    </application>
</manifest>
